# Add build compilation ut sub-directories according to the configured modules:
#
# obj-$(CONFIG_<MODULE>) += <module>

# sliding window
obj-$(CONFIG_SLW)		+= slw

# session descriptor protocol
obj-$(CONFIG_SDP)		+= sdp

# session descriptor protocol
obj-$(CONFIG_VECTOR)		+= vector

